#include <bits/stdc++.h>
#define ll long long
using namespace std;
int main()
{
    ll t, num;
    cin >> t;
    while (t--)
    {
        cin >> num;
        ll x = num%14;
        if ((num>14) && (x>=1 ))
        {
            /* code */
        }
        
    }
    

    return 0;
}